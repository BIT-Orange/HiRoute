# Archived Workflow

`v3_official_rerun.md` documents an older rerun contract.

- active rerun order now lives in `docs/workflows/mainline_workflow.md`
- legacy `v3` runs map to the active families through `runs/registry/experiment_lineage.csv`
