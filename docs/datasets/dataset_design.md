# Dataset Design

The first dataset target is `smartcity_v1`. It will provide a reproducible object corpus, query
set, object-level qrels, hierarchical summary export, and topology mapping for ndnSIM scenarios.

The canonical tracked registry for dataset versions will live in `data/registry/`.
