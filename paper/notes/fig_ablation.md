# Figure: fig_ablation

## Purpose

- Summarize component-level tradeoffs across the current comparison set.

## Built from

- `results/aggregate/ablation_summary.csv`

## Promoted runs

- Current `exp_main_v1` promoted runs.

## Observations

- The plotting pipeline now reserves a dedicated ablation figure path and aggregate contract.

## Caveats

- This is a pipeline-level ablation summary until a dedicated `exp_ablation_v1` promoted run set is added.
