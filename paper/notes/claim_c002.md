# Claim C-002

## Text

On the active `smartcity` `object_main` workload, Figure 5 is an object-resolution result, not a
generic inter-domain superiority claim. The diagnostic failure mix that motivated the rerun was
dominated by `wrong_object`, so the mainline manifest sweep is interpreted as evidence about local
resolution quality under sequential fallback, while deadline summaries remain supporting latency
evidence rather than the primary effectiveness claim.

## Supported by

- `results/figures/mainline/fig_object_manifest_sweep.pdf`
- `results/figures/mainline/fig_deadline_summary.pdf`

## Aggregates

- `results/aggregate/mainline/object_main_manifest_sweep.csv`
- `results/aggregate/mainline/failure_breakdown.csv`
- `results/aggregate/mainline/deadline_summary.csv`

## Source runs

- Promoted runs for `object_main` and `routing_main`
- Legacy `exp_object_main_v2`, `exp_object_main_v3_compact`, and `exp_routing_main_v3_compact`
  provenance is mapped through `runs/registry/experiment_lineage.csv`

## Status

revised
