# Figure: fig_robustness

## Purpose

- Compare service success and overhead under stale summaries and failure scenarios.

## Built from

- `results/aggregate/robustness_summary.csv`

## Promoted runs

- `exp_staleness_v1` and `exp_failures_v1` promoted runs.

## Observations

- The robustness figure path is now bound to scenario and variant metadata from run manifests.

## Caveats

- The committed PDF is currently a placeholder until official staleness and failure runs are promoted.
