# Figure: fig_state_scaling

## Purpose

- Relate exported state growth to topology size and routing scale.

## Built from

- `results/aggregate/state_scaling_summary.csv`

## Promoted runs

- `exp_scaling_v1` promoted runs filtered by topology.

## Observations

- The figure slot is now wired to a concrete aggregate path and PDF target.

## Caveats

- No formal promoted scaling runs are committed yet, so the current PDF is a placeholder awaiting official data.
